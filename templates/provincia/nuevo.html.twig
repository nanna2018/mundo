{% extends 'base.html.twig' %}
{% block body %}
<h1 id="provincia"></h1>

	<h1>Nuevo provincia</h1>

			{{ form_start(formulario) }}
			{{ form_widget(formulario) }}
			{{ form_end(formulario) }}
		<a href="{{path('mundo')}}" class="btn btn-secondary btn-lg active" role="button" aria-pressed="true">Home</a>
       <a href="{{path('provincia_lista')}}" class="btn btn-secondary btn-lg active" role="button" aria-pressed="true">Lista</a>
		
{% block footer%}{% endblock %}
{% endblock %}


