{% extends 'base.html.twig' %}
{% block body %}
<h1>PAISES</h1>
		<ul>
		    {% for region in pais.regiones %}
		        <li>{{ region.nombre}}</li>
		</ul>
<h4>Esta region pertenece a :</h4>

		<li>{{ pais.nombre}}--{{pais.continente}}</li>
    {% endfor %}
     
	<a href="{{path('pais_nuevo')}}">Añadir pais</a>


{% endblock %}